# harness-conventions: repo config (edit freely) — worktree symlink list.
# One repo-relative path per line. post-checkout symlinks each existing file
# from the main worktree into a newly created linked worktree.
# Example entries (replace with this repository's actual gitignored env/secrets):
#.env
#.env.local
#apps/<app>/.env.local
#apps/functions/.secret.local
